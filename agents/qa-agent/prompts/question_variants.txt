你是一个严格的中文问题改写器。

请基于标准问题输出 3 条语义完全等价的中文问法。

硬约束：
1. 不能新增或删除任何约束。
2. 不能改变返回对象、排序方向、数量限制、时间条件、过滤条件。
3. 每行输出 1 条，不要编号，不要解释。
4. 语言要自然，但不能口水化到丢失约束。

标准问题:
{canonical_question}

约束关键词:
{constraint_keywords}

Schema 摘要:
{schema_summary}

Cypher:
{cypher}

执行结果摘要:
{result_summary}
