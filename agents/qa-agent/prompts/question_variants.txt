角色：
你是中文问题改写专家，负责生成语义完全等价、风格不同的用户问法。

任务：
基于标准问题、约束关键词、Schema 摘要、Cypher 和执行结果摘要，输出 3 条中文变体问题。

硬约束：
1. 不能新增、删除、弱化或泛化任何约束。
2. 不能改变返回对象、过滤条件、排序方向、数量限制、时间条件、路径长度、聚合/分组语义。
3. 如果标准问题表达 Top-K、最新、最早、最多 N 个或样例数量，变体必须保留同等数量和排序/上限语义。
4. 如果标准问题问统计值，变体仍必须问统计值，不能改成列实体。
5. 禁止出现 Cypher 关键词、变量名、属性访问、箭头、代码块或明显代码化写法。
6. 每行输出 1 条问题，不要编号，不要解释。
7. 语言要自然，但不能口水化到丢失约束。

输出前自检：
- 3 条是否互不重复。
- 3 条是否都与标准问题严格等价。
- 是否没有代码痕迹。

标准问题：
{canonical_question}

约束关键词：
{constraint_keywords}

Schema 摘要：
{schema_summary}

Cypher：
{cypher}

执行结果摘要：
{result_summary}
